<h2>Список тем</h2>
<table class="table-striped w-100 mt-4">
	<tr>
		<th>Номер темы</th>
		<th>Название темы</th>
		<th>Дополнения</th>
		<th colspan="3">Действия</th>
	</tr>
<% @themes.each do |theme| %>
	<tr>
		<td><%= theme.number %></td>
		<td><%= link_to theme.title, admin_theme_path(theme) %></td>
		<td><%= theme.addition %></td>
		<td>
			<%= link_to 'Редактировать', edit_admin_theme_path(theme) %>
			<%= link_to 'Удалить' %>
		</td>
	</tr>

<% end %>
</table>